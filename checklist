#### 一、 需求清晰 (防御重点：防止信息传递丢失)
 * **业务意图确认：** 需求的真实业务目的是什么？有没有为了解决 A 问题而引入更复杂 B 问题的“伪需求”？
 * **多方共识对齐：** 与产品和后端沟通过程中，是否存在“我觉得他懂了”的隐性假设？（所有关键逻辑必须落在文档或沟通群的结论中）。
 * **数据契约固化：** 接口入参、出参、枚举值是否已彻底明确，并转化为严格的 TypeScript 定义？
#### 二、 流程 (防御重点：防止逻辑断层与死循环)
 * **主干链路 (Happy Path)：** 核心业务数据从输入到提交、流转的完整正向闭环是否顺畅？
 * **异常流转机制：** 接口超时、无网络、Token 过期等异常发生时，系统的降级或错误流转逻辑是什么？
 * **状态管理设计：** 核心业务状态应该放在局部组件内，还是提升到全局 Store？状态流转是否清晰单向？
#### 三、 交互 (防御重点：控制视觉焦点与操作反馈)
 * **中间态反馈：** 所有的异步加载、数据计算、表单提交，是否都有明确的 Loading 或防抖/节流控制？
 * **视觉容错与提示：** 表单校验失败时，视线焦点是否能自动定位到错误字段？错误文案是否通俗易懂？
 * **操作可逆性：** 高危操作（如删除、覆盖）是否有二次确认弹窗？复杂输入流是否有草稿保存机制？
#### 四、 边界 (防御重点：兜底极端场景与系统崩溃)
 * **空与满的极值：** 没有任何数据时（Empty State）长什么样？数据量极大（如复杂表格成百上千行）时会不会卡顿崩溃？
 * **物理尺寸边界：** 接口返回了一段没有空格的超长字符串，会不会把 Flex/Grid 容器撑爆？小屏幕下的响应式排版是否错乱？
 * **权限与安全边界：** 无权限用户直接访问路由是否会被正确拦截？敏感操作的按钮是否被彻底隐藏或禁用？
你可以直接把“需、流、交、边”这四个字写在便签上贴在显示器边缘。每次拿到新任务，按这个顺序在脑子里过一遍，就能自然而然地发现潜在的漏洞。
